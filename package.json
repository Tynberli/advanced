{
    "scripts": {
        "start": "live-server",
        "build": "rollup -c rollup.config.mjs --watch"
    },
    "devDependencies": {
        "liveserver": "^0.1.20",
        "rollup": "^4.13.0"
    }
}